--- # QuickConfigTemplates Version 1.0.2
config:
  yml_directory:
  templates_directory:
  output_directory:
  logging_directory:
  logging_level:
# QuickConfigTemplates-Server Version 1.0.0
server_config:
  protocol: http
  server_host: 192.168.60.2
  server_port: 5000
  server_api_uri: qct/api/v1/

# Logging level is 3 by default, highest is 1 lowest is 5