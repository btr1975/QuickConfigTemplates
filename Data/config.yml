--- # QuickConfigTemplates Version: 1.0.13
config:
  yml_directory:
  templates_directory:
  output_directory:
  logging_directory:
  logging_level:
# QuickConfigTemplates Version: 1.0.13
remote_build_server_config:
  protocol: http
  server_host: 127.0.0.1
  server_port: 5000
  server_api_uri: qct/api/v1/

# Default server_api_uri: 'qct/api/v1/'
# Logging level is 3 by default, highest is 1 lowest is 5